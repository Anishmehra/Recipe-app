import 'package:flutter/material.dart';

class chiefpage extends StatelessWidget {
  const chiefpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("cheifpage"),);
  }
}